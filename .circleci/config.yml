#
#  Globality autogenerated CircleCI configuration
#
#  This file is auto generated with globality-build.
#  You should not make any changes to this file manually
#
#  Any changes made to this file will be overwritten in the
#  next version of the build.
#
#  See: http://github.com/globality-corp/globality-build
#
#

defaults: &defaults
  working_directory: ~/repo
  docker:
    - image: ${AWS_ECR_DOMAIN}/globality-build:fe815e7fe622e84fba3ea7c6f72299c4a052f414
      aws_auth:
        aws_access_key_id: ${AWS_ACCESS_KEY_ID}
        aws_secret_access_key: ${AWS_SECRET_ACCESS_KEY}
      environment:
        EXTRA_INDEX_URL: "InjectedDuringRuntime"
        AWS_ECR_DOMAIN: "InjectedDuringRuntime"
        JFROG_AUTH: "InjectedDuringRuntime"

deploy_defaults: &deploy_defaults
  working_directory: ~/repo
  docker:
    - image: ${AWS_ECR_DOMAIN}/globality-build:fe815e7fe622e84fba3ea7c6f72299c4a052f414
      aws_auth:
        aws_access_key_id: ${AWS_ACCESS_KEY_ID}
        aws_secret_access_key: ${AWS_SECRET_ACCESS_KEY}
      environment:
        EXTRA_INDEX_URL: "InjectedDuringRuntime"
        AWS_ECR_DOMAIN: "InjectedDuringRuntime"
        JFROG_AUTH: "InjectedDuringRuntime"

whitelist: &whitelist
  paths:
    .

version: 2

jobs:
  checkout:
    <<: *defaults

    steps:
      - checkout

      - persist_to_workspace:
          root: ~/repo
          <<: *whitelist

  build_base_docker:
    <<: *defaults

    steps:
      - attach_workspace:
          at: ~/repo

      - setup_remote_docker

      - run:
          name: Build Base Docker
          # install dependencies for loading ecs task definitions
          command: |
            python3 -m venv venv
            . venv/bin/activate
            pip install -U pip
            pip install cookiecutter
            ./scripts/instantiate.sh
            (cd ml-papaya-extractor && globality-build build-gen local)
            eval $(aws ecr get-login --no-include-email)
            globality-build docker-base --repo ml-papaya-extractor --base-dir ml-papaya-extractor
            globality-build docker --repo ml-papaya-extractor --base-dir ml-papaya-extractor

  test:
    <<: *defaults

    steps:
      - attach_workspace:
          at: ~/repo

      - setup_remote_docker
      - run:
          name: Test code
          command: |
            python3 -m venv venv
            . venv/bin/activate
            pip install -U pip
            pip install cookiecutter
            ./scripts/instantiate.sh
            (cd ml-papaya-extractor && globality-build build-gen local)
            docker create -v /src/papaya_extractor/tests/ --name service_tests alpine:3.4 /bin/true
            docker cp $(pwd)/ml-papaya-extractor/papaya_extractor/tests service_tests:/src/papaya_extractor/
            eval $(aws ecr get-login --no-include-email)
            docker pull ${AWS_ECR_DOMAIN}/ml-papaya-extractor:${CIRCLE_SHA1}
            docker run -it  --volumes-from service_tests ${AWS_ECR_DOMAIN}/ml-papaya-extractor:${CIRCLE_SHA1} test
  lint:
    <<: *defaults

    steps:
      - attach_workspace:
          at: ~/repo

      - setup_remote_docker

      - run:
          name: Run Lint
          command: |
            python3 -m venv venv
            . venv/bin/activate
            pip install -U pip
            pip install cookiecutter
            ./scripts/instantiate.sh
            (cd ml-papaya-extractor && globality-build build-gen local)
            docker create -v /src/papaya_extractor/tests/ --name service_tests alpine:3.4 /bin/true
            docker cp $(pwd)/ml-papaya-extractor/papaya_extractor/tests service_tests:/src/papaya_extractor/
            eval $(aws ecr get-login --no-include-email)
            docker pull ${AWS_ECR_DOMAIN}/ml-papaya-extractor:${CIRCLE_SHA1}
            docker run -it --volumes-from service_tests ${AWS_ECR_DOMAIN}/ml-papaya-extractor:${CIRCLE_SHA1} lint

  typehinting:
    <<: *defaults

    steps:
      - attach_workspace:
          at: ~/repo

      - setup_remote_docker

      - run:
          name: Run Typehinting
          command: |
            python3 -m venv venv
            . venv/bin/activate
            pip install -U pip
            pip install cookiecutter
            ./scripts/instantiate.sh
            (cd ml-papaya-extractor && globality-build build-gen local)
            docker create -v /src/papaya_extractor/tests/ --name service_tests alpine:3.4 /bin/true
            docker cp $(pwd)/ml-papaya-extractor/papaya_extractor/tests service_tests:/src/papaya_extractor/
            eval $(aws ecr get-login --no-include-email)
            docker pull ${AWS_ECR_DOMAIN}/ml-papaya-extractor:${CIRCLE_SHA1}
            docker run -it --volumes-from service_tests ${AWS_ECR_DOMAIN}/ml-papaya-extractor:${CIRCLE_SHA1} typehinting

workflows:
  version: 2

  build-and-release:
    jobs:
      - checkout:
          filters:
            # run for all branches and tags
            tags:
              only: /.*/
      - build_base_docker:
          requires:
            - checkout
          filters:
            # run for all branches and tags
            tags:
              only: /.*/
      - test:
          requires:
            - build_base_docker
          filters:
            # run for all branches and tags
            tags:
              only: /.*/
      - lint:
          requires:
            - build_base_docker
          filters:
            # run for all branches and tags
            tags:
              only: /.*/
      - typehinting:
          requires:
            - build_base_docker
          filters:
            # run for all branches and tags
            tags:
              only: /.*/



